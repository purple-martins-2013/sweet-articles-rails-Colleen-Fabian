<% @tags.each do |tag| %>
  <h3><%= link_to tag.name, tag_path(tag) %></h3>
  <ul>
    <% tag.articles.each do |article| %>
      <li><%= link_to article.title, tag_article_path(tag, article) %></li>
    <% end %>
  </ul>
<% end %>
